import { Breadcrumb } from "@components";

const Line = () => {
  return (
    <section className="relative">
      <Breadcrumb pageName="Biểu đồ tuyến đường" />

      <div className="flex flex-col gap-10"></div>
    </section>
  );
};

export default Line;
